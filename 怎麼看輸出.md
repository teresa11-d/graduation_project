# 怎麼看輸出：主控台訊息 + 存檔清單對照表

程式現在預設「精簡模式」（`VERBOSE_DIAGNOSTICS = False`）：主控台只印一行摘要，
完整明細都在存檔的 CSV/JSON/圖檔裡。這份文件對照「看到這行訊息 → 該去哪個檔案看細節
→ 這個數字代表什麼、好壞怎麼判斷」。

想看完整逐折/逐期/逐特徵明細，把 cell 8 的 `VERBOSE_DIAGNOSTICS` 改成 `True` 重跑即可，
邏輯完全一樣，只是印得比較詳細。

---

## 一、主控台訊息，由上到下

| 看到這行 | 意思 | 好壞怎麼判斷 | 細節去哪看 |
|---|---|---|---|
| `[時間性洩漏防範]` | 有沒有排除「募資結束後才可能持續累積」的特徵（img/video/faq 那批） | 正常情況下都會排除，除非你刻意關掉 `EXCLUDE_POST_HOC_RISK_FEATURES` | `post_hoc_retrospective_correlation.csv`：這些被排除特徵跟結果的相關係數，僅供參考 |
| `[洩漏檢查]` | 有沒有單一特徵自己就能把成功/失敗猜到 AUC≥0.95（太準等於可疑） | ✅ 沒有超標 = 正常；⚠️ 有超標 = 要去查那個特徵是不是不小心洩漏了答案 | `feature_diagnostics.csv` |
| `[共線性檢查]` | 特徵彼此之間有沒有互相高度重複（例如兩個定價欄位幾乎是同一個數字） | ✅ 皆低於門檻 = 正常 | `feature_diagnostics.csv` |
| `[共線性處理]` | 上面檢查出問題的特徵，實際被拿掉了幾個 | 拿掉的數量供參考，不代表模型變差，通常是變乾淨 | `feature_diagnostics.csv`（`removed`/`removal_reason` 欄位） |
| `[判別力指標]` | **隨機切分**（同時間段內隨機分訓練/測試）算出來的 AUC——這是最容易「虛高」的一個數字，不要單獨拿這個當結論 | 見下方「AUC 怎麼判斷」表 | — |
| `[市場結構斷點]` | 按季看定價/回饋方案的中位數，有沒有某一季突然跳一大截、之後沒再變回去 | ⚠️ 有斷點 = 市場/平台規則可能換過一套邏輯，全歷史訓練要小心 | `market_structure_over_time.csv`（逐季數字＋`flag_data_quality_suspect`欄位） |
| `[資料異常提醒]` | 樣本量過少＋成功率剛好 0%/100% 的期別，跟「市場真的變了」分開判斷 | 有列出來的期別，建議去查證原始募資頁面再決定要不要用 `MIN_DATE` 排除 | 同上 `market_structure_over_time.csv` |
| `[超參數搜尋]` | 在 walk-forward 折數上試了幾組參數，哪組平均最好 | 只是「候選」，不代表最終會採用，見下面「超參數穩健性檢查」 | `hyperparam_search_experiment_log.csv`（每組候選的完整數字） |
| `[Walk-forward｜xxx]` | 多視窗時間切分驗證：expanding_default（預設參數）/ expanding_best_params（搜尋出的最佳參數）/ sliding_window（固定滑動窗） | 標準差 ≤0.08 算穩定；越穩定代表判別力不是碰運氣 | `walk_forward_results.csv`（逐折）＋`walk_forward_auc.png`（圖） |
| `[特徵分布位移]` | 早期資料 vs 晚期資料，特徵的分布有沒有系統性偏移（PSI≥0.1） | 位移的特徵越多，代表模型學到的東西可能已經不適用現在的市場 | `feature_distribution_shift.csv` + `feature_distribution_shift.png` |
| `[超參數穩健性檢查]` | **這次新增**：最佳參數只在 4 折 walk-forward 上驗證過，這裡額外拿去跟預設參數在「最終單一切分測試」上正面比較，輸的話自動改用預設參數 | ✅ 採用最佳參數 / ⚠️ 改用預設參數，兩種都正常，是機制在把關，不是錯誤 | — |
| `[時間切分驗證｜誠實版-xxx]` | **這才是最重要的數字**：用「過去預測未來」算出來的 AUC，不是隨機切分那個容易虛高的數字 | 見下方「AUC 怎麼判斷」表；同一行也會附「隨機切分對照」跟差距 | `time_based_holdout_check.json` |
| `[誠實版（時間切分）模型 — 機率品質重新驗證]` | 這個模型的**機率數字**準不準（不是排序能力，是「模型說 70% 時是不是真的接近 70%」） | ECE≤0.05 才建議把機率當精確百分比講；KS≥0.3 才算判別力堪用 | `time_based_holdout_check.json` 裡的 `honest_model_calibration` 欄位 |
| `[誠實版單一專案]` | 拿誠實版模型對測試集裡第 `PROJECT_INDEX` 筆專案算出來的機率 | 跟下面「隨機切分版」的機率做對照，量級差太多要留意 | `time_based_single_project_report.json` |
| `[分類別判別力]` | 模型在教育/科技/設計…不同類別裡，是不是每個類別內部都分得出誰會成功 | n 太小（<100）的類別排名不具意義 | 開 `VERBOSE_DIAGNOSTICS=True` 看逐類別數字 |
| `[模型品質指標]` | PR-AUC/F1/KS/Lift 這些補充指標 | 相對 Lift 越接近 100% 代表模型挑高分的能力越好 | 開 `VERBOSE_DIAGNOSTICS=True` 看混淆矩陣等細節 |
| `[機率校準]` | **隨機切分版**模型的機率有沒有套用 Isotonic 校準 | ECE 超過門檻才會套用；套用後 Brier 應該要下降 | `calibration_curve.png` |
| 🎯 單一專案報告區塊 | 隨機切分版模型對測試集第 `PROJECT_INDEX` 筆專案的最終機率、等第、各維度貢獻 | 這是「展示模型怎麼解釋一筆預測」的範例，不是誠實版數字，僅供參考 | `single_project_report.json` |
| 📊 實驗總表摘要 | 把上面所有數字彙整成一張表 | — | `experiment_summary.json`（只放純量數字，不重複塞表格/清單——那些已經各自存成 CSV 了；適合累積存檔比較趨勢） |
| 📁 本次輸出檔案 | 這次跑產生的所有檔案路徑，一次列完 | — | — |

**`train_production_model()` / `validate_recent_window_recipe()` / `print_deployment_recommendation()`**（近期窗口正式模型那幾個 cell）：

| 看到這行 | 意思 |
|---|---|
| `[正式上線模型]` | 只用最近 `MONTHS_BACK_PRODUCTION` 個月訓練出來的模型，AUC/ECE 數字 |
| `[配方穩定性回測]` | 把「近 N 個月訓練」這個做法往回推幾個時間點重跑，檢查配方本身穩不穩，不是只看「現在」這一次 |
| 🚦 部署建議 | 綜合以上所有數字，直接告訴你現在該用哪個 `.joblib` 檔案（或都還不建議用） |

---

## 二、AUC 數字怎麼判斷（經驗法則，`auc_health_check()` 會自動附這句話）

| AUC 範圍 | 意思 |
|---|---|
| ≥0.97 | 🚨 極不合理，幾乎確定有洩漏或訓練/測試重疊 |
| 0.92~0.97 | ⚠️ 明顯偏高，先確認洩漏檢查都乾淨了再採信 |
| 0.70~0.92 | ✅ 落在合理範圍 |
| <0.70 | ⚠️ 偏低，判別力不足 |

## 三、四個上線門檻（決定能不能拿模型機率對外做風險提示）

| 指標 | 門檻 | 去哪個檔案看 |
|---|---|---|
| 誠實版時間切分 AUC | ≥0.70 | `time_based_holdout_check.json` → `time_based_auc` |
| ECE | ≤0.05 | `time_based_holdout_check.json` → `honest_model_calibration.ece` |
| KS 統計量 | ≥0.30 | `time_based_holdout_check.json` → `honest_model_calibration.ks_statistic` |
| Walk-forward 標準差 | ≤0.08 | `walk_forward_results.csv`，或主控台 `[Walk-forward｜xxx]` 那行 |

四項都過，`print_deployment_recommendation()` 才會建議正式採用；沒有全過的話，
它會告訴你哪個模型檔比較接近可用，以及還缺哪一項。

## 四、存檔檔案總表（依產生順序）

| 檔名 | 內容 | 型態 |
|---|---|---|
| `market_structure_over_time.csv` | 逐季成功率＋關鍵特徵中位數＋資料異常標記 | 表格 |
| `hyperparam_search_experiment_log.csv` | 超參數搜尋每組候選的結果 | 表格 |
| `walk_forward_results.csv` | 三種 walk-forward 方法的逐折結果 | 表格 |
| `walk_forward_auc.png` | 上面的視覺化比較圖 | 圖 |
| `feature_distribution_shift.csv` / `.png` | 特徵分布位移排行 | 表格＋圖 |
| `time_based_holdout_check.json` | **誠實版模型**的 AUC/Brier/ECE/KS 等，最重要的一份 | JSON |
| `time_based_single_project_report.json` | 誠實版模型對單一專案的預測 | JSON |
| `feature_diagnostics.csv` | 每個特徵的標籤相關係數＋VIF＋最後有沒有被移除／原因（合併自原本三份檔案） | 表格 |
| `features_used.csv` | 本次訓練實際用到的完整特徵清單 | 表格 |
| `calibration_curve.png` | 隨機切分版模型的可靠度曲線 | 圖 |
| `shap_summary.png` / `shap_importance_bar.png` | 特徵重要性 | 圖 |
| `feature_correlation_heatmap.png` / `feature_label_correlation.png` | 特徵彼此/特徵與標籤的相關係數 | 圖 |
| `single_project_report.json` | 隨機切分版模型對單一專案的預測（含各維度貢獻） | JSON |
| `model_bundle.joblib` | 隨機切分／全歷史訓練出來的模型檔 | 模型檔 |
| `experiment_summary.json` | **所有數字的總彙整**，適合每次重跑後存檔比較 | JSON |
| `model_bundle_production.joblib` | 近期窗口訓練出來的「正式上線」模型檔 | 模型檔 |
| `production_recipe_stability.csv` | 近期窗口配方的穩定性回測結果 | 表格 |

---

## 五、多久看一次、怎麼看

- **平常重跑（例如每次併入新資料）**：只看主控台印出的精簡摘要 + 🚦 部署建議這幾行，
  正常情況下不需要開 `VERBOSE_DIAGNOSTICS`。
- **數字跟預期差很多、想抓原因時**：把 `VERBOSE_DIAGNOSTICS` 改 `True` 重跑，
  或直接開對應的 CSV 檔案看逐筆明細。
- **想追蹤這個模型隨時間有沒有變好/變差**：把每次的 `experiment_summary.json`
  存檔留底（檔名加日期），累積幾次之後可以把裡面的 AUC/ECE 數字拉出來畫成趨勢線。
